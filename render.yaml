services:
  - type: web
    name: emotion-aware-ai-buddy-backend
    env: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    plan: free
    envVars:
      - key: PORT
        value: 8080
      - key: HUGGINGFACE_API_KEY
        sync: false
